{
  "name": "minicode",
  "version": "0.1.0",
  "description": "极致精简的 AI 编码智能体 - 零外部依赖",
  "type": "module",
  "license": "MIT",
  "bin": { "minicode": "./src/cli.ts" },
  "scripts": {
    "dev": "bun run src/cli.ts",
    "start": "node --experimental-strip-types src/cli.ts",
    "models:fetch": "bun run src/cli.ts models",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.3.13"
  }
}
